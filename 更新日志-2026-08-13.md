# 更新日志

## 1. [未发布] 姿态判断算法改进

### 1.1. 摘要

**背景问题**：旧版判定引擎（`judge_pose(kp3d, rule)`）对规则逐条独立计算、每帧全量判定，且只认 3D 坐标。这带来两个直接矛盾：

1. 同一关节在动作的不同阶段应满足不同角度区间——例如哈克深蹲的膝角，蹲到底要求 90–115°，站到顶要求 170–180°，二者互斥。旧引擎没有"阶段"概念，无法让两条规则各自只在对应阶段生效，只能人工在业务层做取舍（要么漏检蹲深、要么全程误报站姿）。
2. 只能用 3D 坐标判定，但 3D 由 MHFormer 基于 351 帧滑动窗口重建，存在延迟与噪声；像高位下拉这类主要靠冠状面（肩外展、水平夹角）就能判断的动作，硬套 3D 反而拖慢且不必要地引入重建误差。

计次同样存在类似问题：旧版直接对原始特征值做阈值直判，2D/3D 关键点的逐帧抖动会让特征值在阈值附近来回跨越，导致方向误判和重复计数。

本次更新据此主要做了三方面重构：

1. 判定引擎从"逐条独立计算、仅 3D"升级为"分组共享计算、2D/3D 双通道、动作阶段门控"，让规则可以按动作阶段和坐标源分别配置；
2. 计次从"阈值直判"升级为"EMA 平滑 + 方向去抖的状态机"，以提供更稳定的计次与阶段（`motion`）数据
3. 计次将会提供阶段信号（`motion`），为姿态判断引擎引入阶段信息，以更精确控制生效时间

主要变动：

- **judge_pose 接口变更**：`judge_pose(kp3d, rule)` → `judge_pose(kp2d, kp3d, rule, motion="")`，接口同时接受 2D/3D 骨骼，并引入阶段信息（motion），以实现按动作阶段门控判定与按需选择骨骼源。
- **rule_set 模式**：规则结构由 `rule_list`（每条独立规则）升级为 `rule_set`（规则组 + `parts`），将相关规则聚拢为同一组，例如下蹲动作的左腿与右腿规则可视为同一组，以此提高规则的组织性与抽象能力。
- **引入 phase 动作阶段**：规则通过 `phase` 字段（如 `["static_down"]`、`["static_up"]`、`["descent", "static_down"]`、`["all"]`）限定生效阶段；这一变动是为了更精确地控制规则的生效时机，仅当当前动作阶段（motion）匹配时才判定。
- **引入 2D/3D 双模式判断**：规则通过 `keypoints_mode`（`"2d"` / `"3d"`）选择数据源；这一变动是为了让不同动作选择合适的骨架源——例如高位下拉几乎只需考虑冠状面情况，此时引入 3D 并无必要，反而影响性能。

> 哈克深蹲、高位下拉迁移至 `rule_set` 新格式，规则文件以 -new 为后缀

### 1.2. 变更详情

#### 1.2.1. 判定引擎重构：rule_set 规则格式

##### 1.2.1.1. 接口变动：judge_pose

判定接口由 `judge_pose(kp3d, rule)` 变更为 `judge_pose(kp2d, kp3d, rule, motion="")`，调用方需要额外传入 2D 关键点与当前动作阶段：

- **新增 `kp2d`**：H36M 格式 2D 关键点（shape=(17, 2) 或 (17, 3)），供 `keypoints_mode: "2d"` 的规则直接基于 2D 像素坐标判定，不再只依赖 3D 重建结果。
- **新增 `motion`**：当前动作阶段字符串（`"descent"` / `"ascent"` / `"static_up"` / `"static_down"`，由 `RepCounter` 提供），作为 phase 门控的匹配输入；默认 `""`，此时仅无 phase 或含 `"all"` 的规则生效。
- **返回值**：`(violated_rule_ids, affected_keypoints)`——被违反的规则 ID 列表（格式 `{action_no}-{rule_no}`）与涉及的 H36M 关节名列表，供 2D 叠加骨架渲染高亮。

##### 1.2.1.2. phase 动作阶段

`phase` 是规则组上的阶段门控字段，把判定限定在动作的特定阶段，避免"全时段一刀切"的误报：

- **取值来源**：匹配 `motion` 四态——`descent`（下降）/ `ascent`（上升）/ `static_up`（顶部静止）/ `static_down`（底部静止），由 `RepCounter` 状态机实时提供。
- **字段形式**：字符串或数组，如 `["static_down"]`、`["descent", "static_down"]`、`["all"]`；数组内可列多个阶段，任一命中即生效。
- **生效规则**：规则组无 `phase` 字段或值为 `["all"]` 时恒生效（每帧都判定）；否则仅当当前 `motion` 命中 `phase` 列表时才参与判定。
- **配置示例**：同类规则可按动作阶段配置不同阈值、互不干扰——哈克深蹲 R1 只在 `static_down`（蹲到底）检查膝角 90–115°，R2 只在 `static_up`（站到顶）检查膝角 170–180°；高位下拉 R1 在 `descent` + `static_down` 阶段检查肩外展。
- **粒度**：`phase` 挂在 rule_set 的规则组上，组内所有 `parts` 继承同一 phase。

以高位下拉 R1 为例（[data/rules/高位下拉-new.json](data/rules/高位下拉-new.json)），`phase` 声明了两个阶段，只要当前 `motion` 命中其中任一即生效：

```json
{
    "rule_no": "R1",
    "phase": ["descent", "static_down"],
    "keypoints_mode": "2d",
    "parts": [
        {
            "rule_type": "夹角",
            "rule_dim": "线段夹角",
            "p1": "胸腔",
            "p2": "左肩",
            "p3": "左肩",
            "p4": "左肘",
            "min_value": 20,
            "max_value": 30
        },
        {
            "rule_type": "夹角",
            "rule_dim": "线段夹角",
            "p1": "胸腔",
            "p2": "右肩",
            "p3": "右肩",
            "p4": "右肘",
            "min_value": 20,
            "max_value": 30
        }
    ]
}
```

这条规则检查左右肩外展角是否落在 20–30°，但只在下拉过程中（`descent`）和拉到底静止（`static_down`）两个阶段生效；到了 `static_up`（顶部放松）或 `ascent`（回升）阶段，`motion` 不在 `phase` 列表内，整组直接跳过判定，不会误报肩外展不足。

##### 1.2.1.3. 新旧对比

|          | 旧版（迁移前）            | 新版（当前）                              |
| -------- | ------------------------- | ----------------------------------------- |
| 规则结构 | `rule_list`，每条独立规则 | `rule_set`：规则组 + `parts` 数组         |
| 接口     | `judge_pose(kp3d, rule)`  | `judge_pose(kp2d, kp3d, rule, motion="")` |
| 数据通道 | 仅 3D                     | `keypoints_mode` 支持 2d / 3d 双通道      |
| 阶段门控 | 无，每帧全量判定          | `phase` 字段，仅当 motion 匹配时生效      |
| 计算方式 | 每条规则独立取点计算      | 可分组规则，同组共享一个 `rule_no`        |

##### 1.2.1.4. 其他改进点

- **规则组 + parts**：一条规则组可含多组关节对（如左右双侧），`parts` 展开后按精确关节名分组，同一组内多条规则（如 min/max 不同的多档阈值）共享一次角度计算，避免重复取点与重复计算。左右两侧的 `parts` 共用同一个 `rule_no`，触发时只报一个规则 ID（如 `04-R1`），不会因为左右腿分别记两条规则 ID 而让违规列表看起来像两个不同问题。
- **2D/3D 双通道**：`keypoints_mode: "2d"` 直接使用 2D 像素坐标判定（如高位下拉的肩外展、肩部水平夹角），不再依赖 3D 重建结果；`"3d"` 使用 H36M 空间坐标。2D 模式下四点坐标补 z=0 后统一走三维几何接口。
- **支持两类规则**：`夹角`（共顶点三点关节角 / 异面两线段夹角）与 `水平夹角`（两点连线与水平面夹角，左右肩高低差检测）。以高位下拉的 R2、R3 为例（[data/rules/高位下拉-new.json](data/rules/高位下拉-new.json)）：

```json
{
    "rule_no": "R2",
    "phase": ["static_up"],
    "keypoints_mode": "2d",
    "parts": [
        {
            "rule_type": "夹角",
            "rule_dim": "线段夹角",
            "p1": "左肩",
            "p2": "左肘",
            "p3": "左肘",
            "p4": "左手腕",
            "min_value": 150,
            "max_value": 180
        }
    ]
}
```

R2 是 `夹角`：p2/p3 都是"左肘"（共顶点），实际算的是左肩-左肘-左手腕三点构成的肘关节角，检查手臂是否伸直到 150–180°。

```json
{
    "rule_no": "R3",
    "phase": ["all"],
    "keypoints_mode": "2d",
    "parts": [
        {
            "rule_type": "水平夹角",
            "p1": "左肩",
            "p2": "右肩",
            "min_value": 0,
            "max_value": 5
        }
    ]
}
```

R3 是 `水平夹角`：只需 p1/p2 两点，算左肩-右肩连线与水平线的夹角，检查两侧肩是否处于同一水平线（0–5° 内视为持平），用于检测高低肩。

##### 1.2.1.5. 新规则文件示例

哈克深蹲（[data/rules/哈克深蹲-new.json](data/rules/哈克深蹲-new.json)）：

```json
{
    "action_no": "04",
    "action_name": "哈克深蹲",
    "rule_set": [
        {
            "rule_no": "R1",
            "phase": ["static_down"],
            "keypoints_mode": "3d",
            "parts": [
                {
                    "rule_type": "夹角",
                    "rule_dim": "线段夹角",
                    "p1": "左髋",
                    "p2": "左膝",
                    "p3": "左膝",
                    "p4": "左脚踝",
                    "min_value": 90,
                    "max_value": 115
                },
                {
                    "rule_type": "夹角",
                    "rule_dim": "线段夹角",
                    "rule_ref": "无",
                    "p1": "右髋",
                    "p2": "右膝",
                    "p3": "右膝",
                    "p4": "右脚踝",
                    "min_value": 90,
                    "max_value": 115
                }
            ]
        },
        {
            "rule_no": "R2",
            "phase": ["static_up"],
            "keypoints_mode": "3d",
            "parts": [
                {
                    "rule_type": "夹角",
                    "rule_dim": "线段夹角",
                    "p1": "左髋",
                    "p2": "左膝",
                    "p3": "左膝",
                    "p4": "左脚踝",
                    "min_value": 170,
                    "max_value": 180
                },
                {
                    "rule_type": "夹角",
                    "rule_dim": "线段夹角",
                    "p1": "右髋",
                    "p2": "右膝",
                    "p3": "右膝",
                    "p4": "右脚踝",
                    "min_value": 170,
                    "max_value": 180
                }
            ]
        }
    ],
    "rep_counting": {
        "type": "angle",
        "p1": "左髋",
        "p2": "左膝",
        "p3": "左膝",
        "p4": "左脚踝",
        "top_threshold": 160,
        "delta_threshold": 0.3,
        "bottom_threshold": 115,
        "count_on": "down_up"
    },
    "calories_per_rep": 0.5,
    "balance_pairs": [
        [
            "左膝",
            "右膝"
        ],
        [
            "左髋",
            "右髋"
        ]
    ]
}
```

#### 1.2.2. 动作阶段判定：motion 四态 + 方向去抖

`phase` 字段要判定生效，得先知道当前处于动作的哪个阶段——这个信号就是 `motion`。考虑到计次与阶段有密切关系，因此将阶段判定的任务放置于 `RepCounter`（core/rep_counter.py）实现，新增 `motion` 属性：

- 四态动作阶段：`DESCENT`（下降中）/ `ASCENT`（上升中）/ `STATIC_UP`（顶部静止）/ `STATIC_DOWN`（底部静止）。区分"运动中"与"静止"两类共四态，是因为规则语义天然需要这个粒度——例如"蹲到底"检查（`static_down`）与"下蹲过程中"检查（`descent`）是两类不同的规则意图，不能合并成一个笼统的"下半程"。
- 瞬时方向（即`DESCENT` / `ASCENT`）由**平滑后特征值的一阶导数**判定（`delta > δ_threshold` → 上升，`< -δ` → 下降，否则按当前相位归为静止态）。方向本质上是"这一帧比上一帧涨了还是跌了"，只能靠逐帧差分（导数）判断，单帧无法看出方向。但如果直接对原始特征值做逐帧差分，关键点本身的抖动会被差分放大成剧烈跳动的方向信号，所以要先对特征值做 EMA 平滑（见下节），再对平滑后的值求差分。
- **方向去抖**：连续 `motion_debounce`（4 帧）同向才切换 motion，过滤单帧抖动。若不去抖，单帧噪声就能让 motion 在两态间来回跳变，phase 门控会被这种抖动瞬间打开又关闭，规则判定失去意义。
- motion 作为 `judge_pose` 的 phase 门控输入，使判定只在正确的动作阶段触发（如对于哈克深蹲，仅 `DESCENT`/`STATIC_DOWN` 阶段才进行进行蹲下角度相关的判断）——这是本次重构解决"背景问题 1"（不同阶段互斥阈值）的关键。

#### 1.2.3. 计次状态机：EMA 平滑 + 双阈值滞回 + ROM 追踪

- **EMA 平滑**（α=0.25）：特征值平滑后再做相位判定，抑制 2D/3D 关键点抖动造成的尖峰误触发。EMA 是必要前提而非可选优化——motion 判定依赖平滑值的导数，若不平滑，导数会被逐帧噪声主导，方向去抖机制形同虚设。
- **双阈值滞回**：特征值 ≥ ceiling → UP（伸展态），≤ floor → DOWN（收缩态），中间区间保持原相位，避免在阈值边界反复翻转。用滞回（双阈值）而不是单阈值，是为了防止特征值在临界点附近的正常波动被反复计成多次动作。
- **可配置触发方向**：`count_on` 支持 `up_down` / `down_up`（哈克深蹲为 `down_up`：蹲到底后伸到顶计 1 次）。可配置是因为不同动作的"完成态"定义不同——有的动作以回到伸展态为完成（深蹲类），有的以到达收缩态为完成（卷腹类）。
- **delta_threshold**：方向切换的特征值变化阈值，默认取 **0.3**。这一取值是实测校准的结果，是用 example-1 的真实 3D 序列统计得出的经验值。如有需要，这一值也可以在规则文件中独立设定。

#### 1.2.4. 2D/3D 双通道流水线

`judge_pose` 现在同时接收 2D 和 3D 骨架单帧数据，使得每条规则可以自主选择规则所需的数据应该从哪种骨骼信息中计算得来：例如，这个角度到底该在图像平面上量，还是该在重建出来的三维空间里量。

3D 是从 351 帧的时序窗口里由 MHFormer 重建出来的，本身就比 2D 慢一步、也更容易带着重建误差。3D 骨架在一些情况下是必须的，例如要判断像深蹲膝角这种在矢状面里前后深度变化很大的动作，2D 像素坐标会因为镜头视角直接把角度看歪，这时候 3D 是必须的，代价换来的是准。

但对于形如高位下拉的肩外展、左右肩高低差这类判断，动作基本都摊在冠状面里，正面拍摄的 2D 坐标已经足够把角度量准，如果这时候还强行要绕一圈走 3D，不仅多背了一份重建延迟和噪声，判断质量却没提升。

规则文件中的 `keypoints_mode` 用于指定规则是使用 2D 骨骼还是 3D 骨骼进行计算。同一份规则文件里不同规则组 2D、3D 规则可以并存，各走各的坐标源，互不影响。

两个例子放在一起看会更直观。哈克深蹲的膝角规则用 3D：

```json
{
    "rule_no": "R1",
    "keypoints_mode": "3d",
    "parts": [
        {
            "p1": "左髋", 
            "p2": "左膝", 
            "p3": "左膝", 
            "p4": "左脚踝",
            "min_value": 90, "max_value": 115
        }
    ]
}
```

高位下拉的肩部水平夹角规则用 2D：

```json
{
    "rule_no": "R3",
    "keypoints_mode": "2d",
    "parts": [
        {
            "rule_type": "水平夹角", 
            "p1": "左肩", 
            "p2": "右肩",
            "min_value": 0, "max_value": 5
        }
    ]
}
```

同样是 `keypoints_mode` 字段，前者选 3D 是因为膝角的深度变化在 2D 下测不准，后者选 2D 是因为肩部水平夹角本来就是平面内的事，不需要 3D 信息。

#### 1.2.5. 规则数据更新

旧格式（`rule_list`）的规则文件保留作参考，两个示例动作迁移到新格式，用以验证新引擎的实际收益：

- **哈克深蹲-new.json**：rule_set 2 组 4 parts——R1 `static_down` 3D 膝角 90–115°（左/右双侧）、R2 `static_up` 3D 膝角 170–180°；`rep_counting` 对齐（top 160 / bottom 115 / `down_up` / δ 0.3）。选 3D 是因为深蹲的膝角判定跨越矢状面，2D 像素坐标下视角变化会直接扭曲角度，必须用 3D 空间坐标才稳定。R1/R2 用两个 phase 分别约束同一膝角，没有 phase 门控之前，这两条规则根本没法同时存在（开头提到的背景问题）。
- **高位下拉-new.json**：rule_set 3 组 5 parts——R1 `descent`+`static_down` 2D 肩外展 20–30°、R2 `static_up` 2D 肘伸展 150–180°、R3 `all` 2D 肩部水平夹角 0–5°。选 2D 是因为这三条规则都发生在正面拍摄的冠状面内，2D 像素坐标已经足够准确，跳过 3D 重建可以减少这条判定链路对 MHFormer 输出延迟的依赖。R1 的 phase 用了两态组合（`descent` + `static_down`），说明 phase 字段按需要覆盖"下降过程中到蹲到底都要检查"这类跨阶段区间，而不仅是单一阶段。

### 1.3. 已知限制

- **劣弧角（180° 回折）**：`angle_between_vectors` / `angle_between_segments` 基于 arccos，只能返回 [0°, 180°]，无法区分 θ 与 360°−θ——例如肘关节实际 190°（过伸）会被计算为 170°，产生"170→180→170"的回折现象。这不是本次重构引入的新问题，而是沿用自旧版就存在的计算方式；但目前此问题尚未引起问题，故可先不考虑。

### 1.4. 涉及文件

- 新增：`core/geometry_math.py`（统一几何模块，尚未嵌入）
- 修改：`core/pose_judger.py`（`judge_pose` 新接口）、`core/rep_counter.py`（`RepCounter` 状态机）、`core/analyzer.py`（2D/3D 双通道流水线）
- 规则数据：`data/rules/哈克深蹲-new.json`、`data/rules/高位下拉-new.json`

#### 1.4.1. kps_analyze_fitness 中的主要改动代码

1. `core/pose_judger.py`
2. `core/rep_counter.py`
3. `core/analyzer.py`
4. `core/audio_guidance.py`

### 附录 1：完整设计

这里按一帧数据实际流经的顺序过一遍整个算法链路

**1）拿到一帧图像，提取 2D 骨骼**

视频源（摄像头或预录 mp4）给出一帧 BGR 图像，交给 `I2dPoseExtractor` 提取 2D 关键点。真机上是 RTMPose，输出 COCO-17 格式；mock 模式下直接读预缓存的 `.npz`。如果提取器输出的是 COCO-17，就用 `DataConverter.coco17_to_h36m` 转成 H36M 索引（11 个关节直接映射，6 个用几何插值补齐）；如果提取器本身就出 H36M（`data_out == "H36M"`），这一步直接跳过。到这里，`kp2d_h36m` 就是本帧唯一需要的 2D 数据，逐帧算完、逐帧可用，没有历史依赖。

**2）滑动窗口喂给 MHFormer，重建 3D 骨骼**

刚转出来的 `kp2d_h36m` 被塞进一个 maxlen=351 的滑动窗口（`self._frame_buffer`）。`I3dPoseReconstructor`（真机是 MHFormer，mock 模式读缓存）吃下整个窗口，重建出这 351 帧对应的 3D 序列，取最后一帧（`frame_index=-1`）作为当前帧的 3D 结果 `kps_3d`。这一步是本帧唯一带时间窗口依赖的环节：`kp2d_h36m` 反映"此刻"，`kps_3d` 反映"此刻在最近 351 帧上下文里应该是什么样"，两者时间语义不完全一致，这也是判定引擎允许按需只用 2D、不强制都走 3D 的原因。

**3）计次状态机吃 3D 数据，产出 motion 和是否计数**

`RepCounter.update(kps_3d)` 每帧都会跑：先用规则里 `rep_counting` 声明的四点（或两点）从 `kps_3d` 算出原始特征值（角度或距离），做 EMA 平滑，再对平滑值求逐帧差分判断瞬时方向，经过 4 帧方向去抖后更新 `motion`（`descent`/`ascent`/`static_up`/`static_down`），同时用平滑值和双阈值滞回判断是否跨过一次完整动作（`rep_counted`），跨过则记一次计数并收一次本次动作的 ROM。这一步只吃 3D，跟某条判定规则是不是配了 `keypoints_mode: "2d"` 无关——即便一个动作所有判定规则都走 2D，它的计次和阶段信号依然要等 3D 重建。

**4）判定引擎按 motion 和 keypoints_mode 分别检查规则**

`judge_pose(kp2d_h36m, kps_3d, self._rule, motion=motion_str)` 拿到这一帧的 2D、3D 坐标和刚算出的 `motion` 字符串。规则文件里的 `rule_set` 先按 `phase` 过滤掉本帧不该生效的规则组（`motion` 不在 `phase` 列表里就整组跳过），剩下的规则按关节名分组（`segment4` 四点或 `horizontal` 两点），每组按 `keypoints_mode` 决定读 `kp2d_h36m` 还是 `kps_3d`，只算一次角度，同组内多条阈值共用这一个结果做区间判断。命中违规就返回规则 ID（`{action_no}-{rule_no}`）和涉及的关节名列表。

**5）违规去抖上报，2D 骨架叠加渲染**

`judge_pose` 每帧都会返回当前违规集合，但不是一违规就立刻上报：analyzer 记录每个违规 ID 第一次出现的帧号，只有连续违规超过 `_debounce_frames`（30 帧，约 1 秒）且还没上报过，才真正加入本帧要对外发的 `violations` 列表；同一段违规只报一次，等它从违规集合里消失才会重新计首帧。渲染这一步用 `H36M2dKeypointsRenderer.render_on_frame`，把 `judge_pose` 返回的关节名反查成 2D 索引（`_map_kp_names_to_indices`），在骨架叠加图上把这些点和相邻连线改成高亮色。

**6）结果打包推送**

一帧的处理结果打包成 `AnalysisResult`：渲染后的图像、`kps_3d`、去抖后的 `violations`、本帧是否计数 `rep_counted`、当前 `motion`。main.py 的 WebSocket 循环拿到这个结果后，把渲染帧编码成 JPEG 推给前端，`kps_3d` 连同 `motion`、`rep_feature_value` 一起以 `kps3d` 消息推给 3D 渲染，`violations`/`rep_counted` 化成日志消息推送，每 30 帧再推一次汇总统计（accuracy / rom / balance_score / density / calories / fatigue_score，这些都是 `FrameAnalyzer` 的属性，按 `RepCounter` 的 `rom_values`/`rep_timestamps` 和违规帧计数算出来的）。

### 附录 2：规则文件详细说明

以哈克深蹲的完整规则文件为例（[data/rules/哈克深蹲-new.json](data/rules/哈克深蹲-new.json)），逐字段说明：

```json
{
    "action_no": "04",
    "action_name": "哈克深蹲",
    "rule_set": [
        {
            "rule_no": "R1",
            "phase": ["static_down"],
            "keypoints_mode": "3d",
            "parts": [
                {
                    "rule_type": "夹角",
                    "rule_dim": "线段夹角",
                    "p1": "左髋",
                    "p2": "左膝",
                    "p3": "左膝",
                    "p4": "左脚踝",
                    "min_value": 90,
                    "max_value": 115
                },
                {
                    "rule_type": "夹角",
                    "rule_dim": "线段夹角",
                    "rule_ref": "无",
                    "p1": "右髋",
                    "p2": "右膝",
                    "p3": "右膝",
                    "p4": "右脚踝",
                    "min_value": 90,
                    "max_value": 115
                }
            ]
        },
        {
            "rule_no": "R2",
            "phase": ["static_up"],
            "keypoints_mode": "3d",
            "parts": [
                {
                    "rule_type": "夹角",
                    "rule_dim": "线段夹角",
                    "p1": "左髋",
                    "p2": "左膝",
                    "p3": "左膝",
                    "p4": "左脚踝",
                    "min_value": 170,
                    "max_value": 180
                },
                {
                    "rule_type": "夹角",
                    "rule_dim": "线段夹角",
                    "p1": "右髋",
                    "p2": "右膝",
                    "p3": "右膝",
                    "p4": "右脚踝",
                    "min_value": 170,
                    "max_value": 180
                }
            ]
        }
    ],
    "rep_counting": {
        "type": "angle",
        "p1": "左髋",
        "p2": "左膝",
        "p3": "左膝",
        "p4": "左脚踝",
        "top_threshold": 160,
        "delta_threshold": 0.3,
        "bottom_threshold": 115,
        "count_on": "down_up"
    },
    "calories_per_rep": 0.5,
    "balance_pairs": [
        ["左膝", "右膝"],
        ["左髋", "右髋"]
    ]
}
```

**顶层字段**

- `action_no`：动作编号，字符串。判定引擎报违规时会拼进规则 ID（`{action_no}-{rule_no}`），如 `04-R1`。也是应用层区分动作的主键。
- `action_name`：动作中文名，仅用于展示，判定逻辑不读这个字段。

**`rule_set`：判定规则组数组**

每个元素是一条独立的规则组，字段含义：

- `rule_no`：规则组编号（`R1`、`R2`……），同一组内所有 `parts` 共用这一个编号，报违规时不区分左右腿分别报，只报一个 `{action_no}-{rule_no}`。
- `phase`：阶段门控，字符串数组。哈克深蹲 R1 只在 `static_down`（蹲到底）生效，R2 只在 `static_up`（站到顶）生效，两者阈值互斥，靠 `phase` 分开。
- `keypoints_mode`：`"2d"` 或 `"3d"`，决定这条规则组用哪一路坐标计算角度。哈克深蹲两组都用 `"3d"`，因为膝角判定跨越矢状面深度变化，2D 像素坐标在视角变化下会失真。
- `parts`：这条规则组实际检查的关节对数组，一条规则组可以有多个 `parts`（哈克深蹲的左腿、右腿各是一个 part），它们共享同一个 `rule_no`/`phase`/`keypoints_mode`，`judge_pose` 会把它们分别单独取点计算角度，但违规时归到同一个规则 ID。

**`parts` 数组每一项**

- `rule_type`：规则类型，目前判定引擎实现了 `"夹角"` 和 `"水平夹角"` 两种（详见 1.2.1.4）。哈克深蹲两组都是 `"夹角"`。
- `rule_dim`：文本描述规则的几何维度（这里都是 `"线段夹角"`），目前只是文档性字段，判定引擎代码里没有读取它，不影响判定逻辑。
- `rule_ref`：个别 part 上出现的文本字段（如 `"无"`），同样是历史遗留、判定引擎不读，可以忽略。
- `p1` / `p2` / `p3` / `p4`：四个关节点的中文名，取值必须是 `pose_judger.py` 里 `JOINT_INDEX` 定义的 17 个 H36M 关节名之一。且要求 **`p2 == p3`**：意味着 `p2`/`p3` 是同一个顶点，实际计算的是以该点为顶点、`p1`→顶点→`p4` 的三点夹角。R1 的 `p1=左髋, p2=左膝, p3=左膝, p4=左脚踝` 就是"左髋-左膝-左脚踝"三点构成的膝关节角。
- `min_value` / `max_value`：角度合法区间（度），闭区间差。R1 是 90–115°（蹲到底膝角不能太浅也不能蹲过头），R2 是 170–180°（站直到接近打直）。当前算出的角度落在 `[min_value, max_value]` 之外才算违规。

**`rep_counting`：计次配置块**

供 `RepCounter` 读取，跟 `rule_set` 是两套独立的配置，字段互不共享（虽然这里 p1~p4 恰好和 R1 用的关节一样，因为都是选膝角做特征值，不是强制要求一致）：

- `type`：特征值类型，`"angle"`（角度）或 `"distance"`（距离）。哈克深蹲用角度。
- `p1` / `p2` / `p3` / `p4`：特征值取点方式跟 `rule_set` 里的 `parts` 一致（`p2 == p3` 即三点角）；`type: "distance"` 时只用 `p1`/`p2`，`p3`/`p4` 可以省略。
- `top_threshold` / `bottom_threshold`：计次状态机的双阈值滞回上下界（详见 1.2.3）。特征值（EMA 平滑后）≥ `top_threshold`（160°）判定为伸展态（UP），≤ `bottom_threshold`（115°）判定为收缩态（DOWN），中间区间维持原状态不翻转。
- `delta_threshold`：方向切换的差分阈值（0.3°/帧），用于从平滑值的逐帧差分判断瞬时方向。
- `count_on`：计数触发方向，`"down_up"` 表示从 DOWN 翻到 UP 才算一次（哈克深蹲：蹲到底再站起来才计 1 次），`"up_down"` 则反过来。

**其他顶层字段**

- `calories_per_rep`：每完成一次动作消耗的估算千卡数，`FrameAnalyzer.calories` 直接拿 `rep_count × calories_per_rep` 算出来，纯查表，不涉及生理模型。
- `balance_pairs`：左右对称关节对的数组，每一项是一对关节名。`balance_score` 用每次动作的 ROM 序列算变异系数（见 `FrameAnalyzer.balance_score`），但目前实现只用了整体 ROM 序列的离散程度，并没有真的按 `balance_pairs` 里指定的关节对分别取左右两侧的数据比较——这个字段目前是预留的配置项，尚未接入计算逻辑。
- 