# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

A Python package named `kps-analyze-demo` (Python >=3.8.19), managed by uv

## Commands

```bash
uv run main.py
```

## Architecture

Single-module project with no dependencies. `main.py` is the entry point.
